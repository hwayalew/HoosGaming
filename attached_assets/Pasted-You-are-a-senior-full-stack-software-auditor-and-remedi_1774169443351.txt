You are a senior full-stack software auditor and remediation engineer. Audit this entire repository end-to-end and then fix it.

Scope:
- frontend
- backend
- API routes
- wiring between UI and API
- all request/response flows
- auth
- DB/storage access
- third-party integrations
- validation
- error handling
- analytics
- deployment-relevant code
- helper utilities
- state management
- feature completeness

Ignore:
- hardcoded app configuration values if intentional
- API keys/secrets stored in `.env`
- complaints that secrets exist in `.env`, unless code exposes them incorrectly

Goals:
1. Build a complete understanding of the app
2. Audit every API and backend function
3. Verify every frontend feature is truly wired and working
4. Identify broken, partial, mocked, misleading, or dead functionality
5. Produce a full audit report
6. Implement fixes directly where possible
7. Add concise summaries/comments so the purpose of each major API is obvious

For every API route or server function, determine:
- purpose
- file path
- who calls it
- inputs
- outputs
- auth requirements
- validation
- dependencies
- current behavior
- broken/missing wiring
- exact fixes needed

For every major page/component, determine:
- what the UI claims to do
- what it really does
- what APIs it depends on
- missing states
- broken actions
- misleading labels
- exact fixes needed

Required outputs:
1. Executive summary
2. System architecture overview
3. Feature inventory table
4. API inventory + audit
5. Frontend wiring audit
6. Backend/service audit
7. Security/reliability findings by severity
8. Database/integration findings
9. Dead code / partial code / misleading code
10. Documentation gaps
11. Prioritized remediation plan
12. File-by-file fix plan
13. Final readiness verdict

Then enter FIX MODE:
- apply the highest-priority fixes first
- preserve architecture where reasonable
- make all APIs work correctly
- make each API’s role clear
- add concise route/module header comments where useful:
  Purpose:
  Called by:
  Input:
  Output:

Be exhaustive and concrete. Do not give shallow feedback. Trace real wiring across files. Flag fake completeness. Distinguish clearly between working, partial, mocked, and broken behavior.